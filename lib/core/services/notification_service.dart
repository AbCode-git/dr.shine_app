import 'dart:developer' as developer;

class NotificationService {
  /// Placeholder for future push notification integration.
  /// Firebase Messaging has been removed. When push notifications are needed,
  /// integrate Supabase Realtime or a dedicated push service here.
  Future<void> initialize() async {
    developer
        .log('NotificationService: Push notifications not configured yet.');
  }
}
